spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fapshi_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "" 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: fapshi-backend

  server:
    port: 8080
    address: 0.0.0.0 // pour permettre la connexion depuis l'extérieur comme mon un telephone 

  # =============================================
  # CONFIGURATION EMAIL - À REMPLIR ICI
  # =============================================
  mail:
    host: smtp.gmail.com
    port: 587
    username: pewoparfait@gmail.com                   # ← METS TON EMAIL GMAIL ICI
    password: nzzinpcmwaauxdja                        # ← METS TON APP PASSWORD ICI (16 caractères, SANS ESPACES)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# =============================================
# CONFIGURATION LOGS (déjà bien)
# =============================================
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.fapshi.backend.security: DEBUG

# =============================================
# CONFIGURATION SWAGGER (déjà bien)
# =============================================
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# =============================================
# CONFIGURATION SUPPLÉMENTAIRE (optionnelle mais utile)
# =============================================
app:
  reset-code:
    expiration-minutes: 10  # Durée de validité du code de réinitialisation